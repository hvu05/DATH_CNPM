model ProductSpec{
  id Int
  product_id Int
  spec_name String
  spec_value String

  @@id(name: "spec_id",[id,product_id])
  product Product @relation(fields: [product_id], references: [id])
}