model ProductImage{
  id Int           
  product_id Int
  image_url String
  image_public_id String
  is_thumbnail Boolean

  @@id(name: "image_id",[id, product_id])

  product Product @relation(fields: [product_id], references: [id])
}