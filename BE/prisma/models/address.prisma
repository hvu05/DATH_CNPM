model Address{
  id Int
  user_id String
  province String
  ward String
  detail String
  receive_name String?
  phone String?
  @@id([id,user_id])
  user User @relation(fields: [user_id],references: [id])
}